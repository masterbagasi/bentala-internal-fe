// Remotion entry point — registered as the project's "remotion entry" via
// remotion.config.ts. Imports compositions by side-effect (registerRoot).
import { registerRoot } from 'remotion'
import { RemotionRoot } from './Root'

registerRoot(RemotionRoot)
